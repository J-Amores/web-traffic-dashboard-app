2026-06-06T13:23:35Z | architect | Skeleton verified GREEN for feature "Country hover mini-dashboard". build OK, typecheck OK (fixed: added `docs` to tsconfig exclude — stray untracked v0 scaffold was breaking the gate; 0 errors in app code), lint clean, health 200 (79011 rows), smoke all-pass. Per-country invariant proven: /api/kpis?country=United States sessions.current=16906 == /api/geo count=16906. Backend unchanged. Substrate templates instantiated (CLAUDE.md, CODEBASE_MAP.md). Wrote .harness/api-contract.md. Ready for frontend.
2026-06-06 | frontend | Built country hover mini-dashboard. NEW components/console/CountryPanel.tsx (per-country fetch of /api/kpis + /api/breakdown/{devices,channels,referrers} + /api/pages with ?period=all&country=<C>, typed via lib/types.ts; module-level per-country CACHE + request-id stale-guard; loading skeleton, error state on fetch failure, prefers-reduced-motion honored). Wired components/console/DottedMap.tsx: replaced the simple tooltip with hotspot mouseenter/leave -> AnimatePresence-mounted CountryPanel (120ms leave grace; panel is pointer-events-none so it never traps hover). Panel flips left past 55% width and up past 55% height to stay inside the map's overflow-hidden box; flip translate + entrance offset expressed via framer x/y (inline transform is clobbered by motion). Reused --ds-* tokens, Geist Mono, tabular-nums; no new deps, no API/SQL/seed changes. Extended scripts/verify-ui.mjs to iterate hotspots via real .hover() (many anchors project off-screen), assert panel header+KPI numerals+7 section labels, and cache re-hover replay. GATE: typecheck OK, lint clean, build OK, smoke all-pass. Runtime (playwright IS installed locally): panel opens with REAL per-country data and header == /api/geo count == kpis.sessions.current (verified US=16906, Germany=6797, Australia=2295); forced-500 renders the error state without crashing; reduced-motion context renders with 0 pageerrors; cache re-hover shows content with no loader re-flash. Note for future runs: `npm run build` clobbers the running `npm run dev`'s .next and 500s it -- run runtime/visual verification on a dev server started AFTER the last build (or `rm -rf .next` + restart).
